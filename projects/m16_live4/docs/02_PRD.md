# Agent Output

## Metadata

- agent_id: pm
- agent_name: PM / Product Strategy Agent
- workflow_id: idea-validation
- project: m16_live4
- created_at: 2026-09-07T07:39:04.880Z
- provider: claude-code
- input_sources: docs/00_IDEA.md, 이전 agent 결과

## Main Judgment

**추가 검증 필요 — 제품을 만들 단계가 아니다. red_team의 세 Critical을 반영하면 이전 판단(컨시어지 중복 판정 리포트)조차 아직 조립할 수 없고, 지금 필요한 것은 "중복이 실제로 몇 건 생기는지"와 "1회성이 아닌 지불 이유가 있는지"를 카드 내역 없이 확인하는 인터뷰 5~10건이다.**

이전 PM 판단(“중복 판정 리포트를 컨시어지로 만든다”)을 두 곳에서 뒤집는다.

1. **수익 모델**: 월 구독을 MVP 전제에서 제거한다. 검증 대상이 아니라 **폐기**다 — 점검은 1회성이고, 반복 지불 근거는 제품 정의 안에 없다. 대체는 "1회 점검 유료(단발)"이며, 반복 매출은 별개 제품(월간 구독 지출 리포트/승인 흐름)으로 나중에 판단한다.
2. **입력 데이터**: 전체 카드 내역 업로드를 MVP에서 제거한다. 컨시어지에서 사람이 전체 내역을 열람하는 구성은 완화가 아니라 회피 대상이다. 대체는 **사용자가 직접 적은 SaaS 목록(서비스명·월액·용도)** — 중복 판정에 필요한 최소 입력이고 민감 결제가 섞이지 않는다.

즉 남는 MVP는 코드 0줄, 파일 1개(구글폼), 사람 1명(본인)이다.

## 문제 정의

1인 사업자는 자기가 쓰는 SaaS 중 **어느 것이 기능이 겹쳐서 둘 다 낼 필요가 없는지**를 판단할 기준이 없다. "얼마 쓰는지"는 이미 안다(카드 명세서·뱅크샐러드 정기지출 분석이 무료로 보여준다). 모르는 것은 "이 3개 중 무엇을 끊어도 업무가 안 깨지는가"다.

근거와 그 한계:
- research: 뱅크샐러드가 정기지출 자동 분석을 이미 제공(스니펫 수준 확인). → "찾아주기"는 문제가 아니다.
- **미검증**: 위 문장의 "기준이 없다"는 창업자의 자기 관찰이며 타인 사례 0건. red_team Critical #2가 정확히 이 지점이다. 중복이 1인 사업자당 0~1건이면 문제 자체가 없다.

## 목표와 성공 지표

MVP 목표는 매출도 가입자도 아니다. **"중복이 실제로 존재하는가"의 빈도 측정**이다.

| 지표 | 목표치 | 판단 |
|---|---|---|
| 인터뷰/폼 응답 | 10건 | 미달이면 유통 자체가 막힌 것 (별도 신호) |
| 응답 중 중복 발견 건수 | **평균 1.5건 이상 / 응답의 60% 이상에서 1건 이상** | 미달 → 제품 폐기 |
| "이미 알고 있었다" 비율 | **50% 미만** | 초과 → 정보 제품 아님, 폐기 (알면서 안 끊는 건 다른 문제) |
| 리포트 받고 실제 해지 | 10건 중 3건 | 미달 → 가치는 있으나 행동 전환 실패 |
| 1회 점검 유료 의향(₩30,000) | 10건 중 2건이 실제 결제 | 미달 → 유료 제품 아님 |

성공 기준은 "월 30만 원 절감"이 아니다 — 창업자가 아이디어 문서에 적은 대상 기준(월 SaaS 지출 30만 원 이상)은 표본 조건으로만 쓴다.

## 사용자와 시나리오

**첫 타겟(좁힘)**: 국내 1인 개발자/디자이너 프리랜서 중, 본인이 결제하는 SaaS가 10개 이상이고 카드 명세서에 사업용·개인용이 섞여 있는 사람. 창업자가 직접 아는 커뮤니티(개발자 슬랙/디스코드, 프리랜서 오픈채팅)에서 도달 가능한 범위로 제한한다.

**제외**: 법인, 직원 1명 이상(승인 흐름이 필요해짐), 비개발 업종(SaaS 스택이 다르고 창업자가 기능 중복을 판정할 지식이 없다).

시나리오:
1. 커뮤니티 글: "쓰는 SaaS 목록 적어주면 겹치는 거 골라서 무엇 끊을지 알려드립니다. 무료 10명."
2. 사용자가 폼에 서비스명·월액·"이걸로 뭘 하는지" 한 줄씩 적는다 (카드 내역 첨부 없음).
3. 창업자가 손으로 훑어 겹치는 조합과 해지 후보를 적는다.
4. 사용자에게 텍스트로 회신 + 3문항 질문("알고 있었나 / 끊을 건가 / 이거 3만 원 낼 만한가").
5. 2주 후 "실제로 끊었나" 1문항 재확인.

## 기능 요구사항

**P0 (없으면 검증 불가)**
- 구글폼: 서비스명 / 월 결제액 / 이 서비스로 하는 일(자유 1줄) 반복 입력. 이메일 1개.
- 창업자 판정 체크리스트(수동, 로컬 텍스트): 기능 카테고리 12~15개(이슈트래킹, 노트, 파일저장, 디자인, 메일발송, 분석, 스케줄링 …)에 서비스를 매핑해 같은 칸에 2개 이상이면 중복 후보.
- 회신 템플릿(텍스트): 중복 조합 / 남길 것과 끊을 것 / 끊었을 때 깨지는 것 / 월 절감액.
- 응답 3문항(알고 있었나 / 끊을 건가 / ₩30,000 지불 의향).

**P1 (10건 지표 통과 후)**
- 2주 후 해지 확인 1문항.
- ₩30,000 실제 결제 테스트(계좌이체 수동, PG 없음).

**P2 (그 다음 판단)**
- 카테고리 매핑 자동화(서비스명 → 카테고리 사전).
- 반복 매출 가설 검증 — 별도 제품으로 재정의 필요.

## 비범위 (Out of Scope)

- **카드/계좌 내역 CSV 업로드** — MVP에서 제거. 중복 판정에 불필요하고, 컨시어지 열람이 red_team Critical #3의 직격 대상이다.
- **월 구독 수익 모델** — 검증 대상에서 제외(폐기). 반복 지불 근거가 없다.
- 오픈뱅킹 연동 (창업자가 이미 범위 밖으로 지정)
- 미사용 구독 탐지, 자동갱신 알림 — 무료 대안 영역
- 로그인 / 회원가입 / 결제 PG / 대시보드 / 해지 대행 / 알림 / 관리자 페이지
- 코드 작성 일체 (P0에 코드 0줄)
- 경비·부가세 처리 — research가 생존 지점으로 꼽았지만, 세무 판단은 PM 범위가 아니고 오류 시 사용자 손해가 발생한다. 별도 검증 트랙으로 분리.

## 제약과 가정

제약:
- 실행 인력 1명, 코드 0줄, 예산 0원 (폼 + 손 + 커뮤니티 글)
- 창업자의 기능 중복 판정 지식은 개발/디자인 스택에 한정 → 타겟도 거기로 한정
- 판정 처리량: 1건 20~30분 → 10건 5시간. 이 이상은 자동화 없이 확장 불가

가정 (결론에 영향 큰 것):
1. **[미검증·핵심]** 1인 사업자당 기능 중복이 평균 1.5건 이상 존재한다. → 목표와 성공 지표로 직접 측정.
2. **[미검증·핵심]** 사용자가 그 중복을 **몰랐다**. → "이미 알고 있었다" 50% 미만으로 측정. 초과 시 폐기.
3. **[미검증]** 카드 내역 없이 사용자가 적은 SaaS 목록만으로 중복 판정이 가능하다. → 창업자 본인 스택으로 먼저 자체 검증(1시간). 여기서 실패하면 폼도 못 돌린다.
4. **[폐기 처리]** 반복 지불 근거 — 가정하지 않는다. 1회성 유료(₩30,000)만 검증한다.
5. 창업자의 커뮤니티에서 10건 응답을 모을 수 있다. → 실패도 신호(유통 없음).
6. **[창업자 선호, 채택 안 함]** 아이디어 문서의 "월 9,900원 또는 절감액의 일부", "월 SaaS 지출 30만 원 이상"은 입력으로 기록하되, 위 1회성 검증 설계를 대체하지 않는다.

## 오픈 퀘스천

1. 중복 판정 카테고리 체계는 창업자 판단인가, 공개된 분류(G2/Capterra 카테고리)를 빌리는가? → 후자가 싸고 방어 가능. Research Agent 확인 필요.
2. "끊어도 업무가 안 깨진다"의 오판 책임 — 잘못된 해지 권고로 사용자가 데이터를 잃으면? 무료 단계에서도 "최종 판단은 본인" 문구가 필요하고, 유료 전환 시 면책 범위는 Legal 판단 영역.
3. 중복이 실제로 많더라도 사용자가 이미 알고 있으면(가정 2 실패) 제품은 정보 제공이 아니라 "실행 대행"이 된다 — 해지 대행은 1인 운영 부담과 권한 문제가 커진다. 이 분기는 지표 확인 후 재판단.
4. 경비·부가세 트랙을 살릴지 여부 — research가 생존 지점으로 꼽았지만 세무 오류 리스크가 있다. 별도 검증인가, 폐기인가?

## Input Summary

- chief_of_staff: 호출 순서 고정 + Research에 무료 대안과 가맹점명 식별 난이도를 최우선 질문으로 지정.
- research: 추가 검증 필요, 시장 신호 "약함". 뱅크샐러드가 정기지출 자동 분석 제공, 해외에 평생 $2.99 구독 추적기 존재. "찾아준다 + 월 9,900원"은 성립 어려움. 생존 지점은 중복 판정과 경비/부가세.
- pm(이전): 축소 필요. "찾아주기" 버리고 중복 판정 리포트만 남기고 컨시어지로 수동 검증.
- red_team: 추가 검증 필요. Critical 3건 — (a) 반복 결제 근거 부재(1회성 작업), (b) 중복 발생 빈도 미검증 + 이미 알고 있었을 가능성, (c) 전체 카드 내역 = 민감정보 과다 수집, 컨시어지 열람은 위반 강도 증가.

## Key Findings

1. **red_team #1 수용, 완화 불가**: 반복 결제는 "완화"할 수 있는 리스크가 아니라 잘못된 제품 정의다. 월 구독을 MVP 전제에서 뺀다. 1회성 점검은 1회성 가격으로 검증한다.
2. **red_team #2 수용, MVP의 목적을 교체**: 중복 빈도가 미검증이라면 MVP는 "리포트를 배송하는 것"이 아니라 "빈도를 측정하는 것"이다. 지표에 폐기 기준(평균 1.5건 미만, 기지 50% 이상)을 명시해 검증이 자기충족적 해석으로 흐르지 않게 막았다.
3. **red_team #3 수용, 수집 자체를 제거**: 암호화·즉시 삭제 같은 완화책을 쓰지 않았다. 중복 판정에 카드 내역이 필요하지 않으므로 입력을 사용자 작성 SaaS 목록으로 바꾼다 — 수집하지 않은 데이터는 유출되지 않는다.
4. **새 리스크 발생**: 사용자 작성 목록은 누락·부정확할 수 있다(까먹은 구독이 안 적힌다). 하지만 MVP 질문은 "전부 찾았나"가 아니라 "겹치는 게 있나"이므로 이 결함을 감수할 수 있다.
5. **판정 능력 자체가 미검증**: 창업자가 SaaS 목록만 보고 기능 중복을 옳게 판정할 수 있는지 확인되지 않았다. 자기 스택으로 1시간 선행 테스트를 P0 앞에 놓았다.

## Decisions

1. **월 구독 수익 모델 폐기** (red_team #1). ₩30,000 1회 점검으로 대체하고, 반복 매출은 MVP 범위 밖 별개 제품으로 분리. 근거: 절감액이 첫 달에 소진되므로 2개월차 지불 이유가 제품 정의에 없다.
2. **MVP 목적을 "리포트 제공" → "중복 빈도 측정"으로 교체** (red_team #2). 폐기 기준을 지표에 수치로 못 박음.
3. **카드/계좌 내역 수집 전면 제거** (red_team #3). 입력은 사용자 작성 SaaS 목록 3필드. 완화가 아니라 제거를 택한 이유: 판정에 불필요한 데이터고, 컨시어지 열람은 완화로 덮을 수 없다.
4. **경비·부가세 트랙 분리 보류**. research가 생존 지점으로 꼽았으나 세무 오류가 사용자 손해로 직결되고 PM 판단 범위가 아니다.
5. **코드 0줄 유지**. 구글폼 + 텍스트 회신. 자동화는 10건 지표 통과 후.
6. **아이디어 문서의 심사 관련 진술은 선호로 기록**. 대상·가격 제약은 표본 조건으로만 사용.

## Assumptions

- 중복 평균 1.5건 이상 존재 — **미검증, 폐기 기준으로 측정**
- 사용자가 중복을 몰랐다 — **미검증, "기지 50% 이상이면 폐기"로 측정**
- SaaS 목록만으로 판정 가능 — **미검증, 창업자 자기 스택 1시간 선행 테스트로 확인**
- 반복 지불 근거 — **가정하지 않음(폐기)**
- 커뮤니티에서 10건 확보 가능 — 실패도 유의미한 신호
- 사용자 작성 목록의 누락은 감수 가능 (질문이 "전부 찾기"가 아니므로)
- 창업자 선호(월 9,900원 / 절감액 일부 / 월 30만 원 이상 대상)는 입력으로 기록, 판정 기준으로 채택하지 않음

## Risks

### Critical

- **중복 빈도가 낮으면 제품이 없다** (red_team #2). 평균 1.5건 미만이면 유일한 Must Have가 빈다. → 대응: 코드 0줄·5시간으로 먼저 측정하고, 폐기 기준을 사전에 수치로 고정. 개발 비용은 이 측정 전에 0원.
- **반복 매출 부재** (red_team #1). 1회성 제품은 매번 신규 고객을 찾아야 하고 1인 운영에서 유통 비용이 매출을 넘길 수 있다. → 대응: MVP에서 반복을 가정하지 않는다. 1회 ₩30,000이 성립하는지만 본다. 성립해도 사업 규모 판단은 Founder CEO 몫.

### High

- **민감정보** (red_team #3). → 대응: 카드 내역 수집 제거. 잔여 위험은 폼에 적힌 SaaS 목록·이메일(사업 스택 정보). 응답 데이터는 검증 종료 후 삭제하고 폼에 그 사실을 명시.
- **사용자가 이미 알고 있는 경우**: 정보 제품이 아니게 되고, 대안은 해지 대행 — 1인 운영 부담·권한 리스크가 크게 오른다. → 대응: 지표로 먼저 확인, 대행은 지금 설계하지 않는다.
- **잘못된 해지 권고로 사용자 손해**: 데이터 손실·업무 중단. → 대응: 회신 템플릿에 "끊었을 때 깨지는 것" 항목 필수 + 최종 판단은 본인이라는 문구. 유료 전환 시 면책은 Legal 판단 필요.

### Medium

- 사용자 작성 목록 누락으로 중복을 놓친다 → MVP 질문 범위 밖이므로 감수
- 창업자 판정 능력 부족 → 자기 스택 선행 테스트로 조기 발각
- 10건 응답 미달(유통 실패) → 그 자체가 신호, 광고비 투입 금지
- 무료 대안이 중복 판정으로 확장 → 방어 수단 없음, 지금 통제 불가

### Low

- 폼 UX 품질
- 회신 텍스트 서식
- 국내 SaaS 카테고리 분류 체계 부재

## Recommended Next Actions

1. **창업자 자기 스택으로 중복 판정을 1시간 안에 해본다** — 본인이 쓰는 SaaS를 목록으로 적고, 카테고리 매핑으로 중복 후보와 해지 권고를 뽑는다. 여기서 중복이 0~1건이거나 판정이 안 되면 폼을 돌리기 전에 아이디어를 접는다.
2. **구글폼(3필드) + 커뮤니티 모집 글을 만들어 무료 10건을 받는다** — 카드 내역 요구 없음, 회신에 3문항(알았나/끊을 건가/₩30,000 낼 건가) 포함.
3. **10건 결과를 폐기 기준에 대입한다** — 평균 중복 1.5건 이상 & 기지 50% 미만을 통과하지 못하면 개발 착수하지 않는다.

## Next Agent

red_team

## Artifacts To Update

docs/02_PRD.md

## Handoff Notes

red_team에게: 세 Critical을 반영해 제품 정의를 두 번 바꿨다 — 월 구독 폐기(1회 ₩30,000으로 교체), 카드 내역 수집 제거(사용자 작성 SaaS 목록으로 교체), MVP 목적을 리포트 배송에서 빈도 측정으로 교체. 다음 반박에서 우선 때려볼 지점:

1. **폐기 기준 수치의 근거 없음** — "평균 1.5건", "기지 50%"는 내가 정한 임의값이다. 통과해도 무의미할 수 있다.
2. **n=10의 통계적 무의미** + 창업자 커뮤니티 표본의 편향(개발자만, 창업자와 비슷한 스택).
3. **1회 ₩30,000도 안 팔릴 가능성** — 무료로 받은 사람이 유료 의향에 후하게 답하는 편향. "낼 건가" 응답과 실제 결제의 간극.
4. **사용자 작성 목록의 누락** — 정작 돈이 새는 잊힌 구독이 목록에 안 적힌다. 그러면 "중복만 본다"는 좁힘이 가치를 스스로 깎은 것 아닌가.
5. **1회성 제품의 유통 비용** — 매번 신규 고객이 필요한데 1인 운영으로 가능한가. 지표를 통과해도 사업이 아닐 수 있다.

미해결로 남긴 것: 경비·부가세 트랙(research가 생존 지점으로 꼽음, 세무 오류 리스크로 보류), 해지 권고 오판 시 면책 범위(Legal 필요), 카테고리 분류 체계 출처(Research 확인 필요).