# Agent Output

## Metadata

- agent_id: chief_of_staff
- agent_name: Chief of Staff Agent
- workflow_id: dev-preflight
- project: _t_preflight
- created_at: 2026-09-03T04:46:37.075Z
- provider: claude-code
- input_sources: docs/00_IDEA.md, 이전 agent 결과

## Input Summary

앞선 6개 에이전트가 사내 문서 통합 검색 MVP를 검토했다. 결과를 성격별로 나누면 셋이다.

**착수 조건(선행 결정이 필요한 것) — 2건**

- tech_lead: SQLite FTS5 단일 파일 + CLI 재색인으로 1인 2~3주 규모. 단 **"문서별 권한을 구현하지 않는 대신 색인 대상을 전 직원 열람 가능 소스로 allowlist 제한"** 결정이 선행되지 않으면 착수 금지.
- red_team: **추가 검증 필요**. 위 allowlist 축소가 검색 가치가 가장 높은 문서를 색인에서 빼고, 현재 MVP(위키 export + 로컬 PDF)는 문제 진술이 지목한 흩어짐(슬랙·드라이브)을 커버하지 않는다. 코드 전에 "축소 후 남는 가치"를 확인하라.

**에이전트 간 충돌 — 1건**

- spawn_indexer: 재색인은 **임시 테이블에 전부 쓰고 마지막에 한 번 교체**해야 한다. 이유는 야간 cron이 중간에 죽으면 색인이 반쯤 빈 채 아침까지 아무도 모른다는 것.
- spawn_search_api: **그 교체 기구를 폐기하라.** SQLite 트랜잭션이 이미 원자성을 주므로 문제를 두 번 푸는 것이고, external-content FTS5의 트리거·rowid 결합을 깬다.

**논쟁 없는 착수 시 반영 항목 — 5건**

- spawn_search_api: 색인·조회 양쪽에서 한국어 NFC 정규화 (NFD 문서는 trigram에서 영원히 검색 불가).
- spawn_web_ui: 스니펫 하이라이트를 HTML이 아닌 제어문자 구분자로 → 페이지에서 `innerHTML` 제거.
- spawn_web_ui: 300ms debounce 폐기, `<form>` 제출로 검색 (코드 감소 + `hit_count=0` 지표 오염 방지).
- spawn_access_guard: allowlist를 DB `sources` 테이블이 아닌 `sources.json`으로, 파일 없으면 색인기 실행 거부.
- spawn_access_guard: `X-Auth-User`를 신뢰 경로 확인 + 형식 검증 2단계로, `TRUST_PROXY_HEADER=1` 미설정 시 서버 부팅 거부.

## Main Judgment

**코드 착수 전에 닫아야 할 것은 두 개뿐이다 — ① 색인 allowlist 확정(= red_team의 "축소 후 남는 가치" 확인과 tech_lead의 선행조건은 같은 하나의 결정이다), ② FTS5 쓰기 방식 단일 소유자 지정(indexer↔search_api 충돌). 나머지 5개 권고는 논쟁 대상이 아니므로 착수 시점에 그대로 반영하면 되고, 이 둘 때문에 4개 구현 트랙 전부를 멈출 필요는 없다.**

①은 문서 작업 반나절, ②는 판정 30분짜리다. 지금 이 워크플로우가 만들어낸 가장 큰 낭비 위험은 "6개 에이전트가 각자 착수 조건을 붙여서 아무도 시작하지 못하는 상태"이며, 그건 조율 문제이지 기술 문제가 아니다.

## Key Findings

**1. tech_lead의 선행조건과 red_team의 "추가 검증"은 별개 작업이 아니다.**
tech_lead는 "allowlist로 제한하기로 결정하라"고 했고, red_team은 "그렇게 제한하면 남는 가치가 있는지 먼저 확인하라"고 했다. 둘 다 **동일한 산출물 하나**로 닫힌다: 색인 대상 소스 목록을 실제로 열거하고, 그 목록만으로 검색했을 때 답이 나오는 실제 질문 5~10개를 적어보는 것. 두 개의 게이트로 취급하면 일정만 두 배가 된다.

**2. indexer↔search_api 충돌은 실은 두 개의 다른 질문이 섞인 것이다.**

- (a) *쓰기 방식* — 임시 테이블 교체인가, 실제 테이블 단일 트랜잭션인가. 이건 스키마 소유권 문제이며 한 명이 판정해야 한다.
- (b) *실패 감지* — "중간에 죽으면 아침까지 아무도 모른다"는 indexer의 실제 걱정. 이건 (a)가 어느 쪽으로 결정되든 **해결되지 않는다.** 트랜잭션이 롤백되면 색인은 온전하지만 여전히 **낡은 상태**이고, cron은 조용히 실패한다.

(b)를 (a)에 얹어서 풀려 했기 때문에 교체 기구가 나왔다. 분리하면 (b)는 "재색인 종료 시 처리 문서 수를 로그에 남기고, 0건이거나 예외로 끝나면 non-zero exit + cron 메일" 몇 줄로 닫힌다. 스키마와 무관하다.

**3. 4개 구현 트랙 중 병렬 가능한 것은 하나뿐이다.**
저장소 규약상 병렬은 격리 worktree + 파일 소유권 분리가 성립할 때만이다.

- `spawn_web_ui` — HTML 1장, 서버 계약(제어문자 구분자 스니펫)만 고정되면 **독립. 병렬 가능.**
- `spawn_search_api` ↔ `spawn_access_guard` — 부팅 거부와 헤더 검증은 **같은 서버 파일**을 만진다. **직렬, 또는 한 세션이 둘 다.**
- `spawn_indexer` ↔ `spawn_access_guard` — `sources.json` 스키마를 공유한다. **스키마 문자열만 먼저 고정하면 이후 병렬 가능.**

**4. red_team의 범위 지적(슬랙·드라이브 미커버)은 이 워크플로우에서 닫을 수 없다.**
MVP 범위를 넓힐지 문제 진술을 좁힐지는 PM/Founder CEO 판단이다. 이 워크플로우에는 그 단계가 없다. **다음 워크플로우로 넘기되, 그 판단이 나올 때까지 위 항목 1~3을 멈추지 않는다** — 위키+PDF 색인 코드는 소스가 늘어나도 버려지지 않기 때문이다.

**5. 사용자가 놓쳤을 수 있는 것: 이 아이디어에는 "검색 품질을 무엇으로 판단하는가"가 여전히 비어 있다.**
`docs/00_IDEA.md` 미해결 항목에 있고, web_ui가 `hit_count=0` 로그를 유일한 품질 지표로 지목했지만 **아무도 그 지표의 합격선을 정하지 않았다.** 출시 후 "검색이 별로다"라는 피드백이 왔을 때 되돌아볼 기준이 없다.

## Decisions

이 에이전트가 내리는 것은 **실행 순서·소유권 결정**이며, 사업 판단(진행/폐기)이 아니다.

| # | 결정 | 근거 |
|---|---|---|
| D-1 | tech_lead 선행조건과 red_team 검증 요구를 **하나의 산출물**(`docs/01_INDEX_SCOPE.md`)로 통합한다 | 같은 결정에 대한 두 개의 표현. 게이트를 두 번 세우면 일정만 늘어난다 |
| D-2 | FTS5 쓰기 방식 판정의 **단일 소유자는 tech_lead**로 지정한다 | 공유 스키마 변경은 직렬·단일 소유자 원칙. indexer/search_api 둘 다 당사자라 자기 판정 불가 |
| D-3 | indexer의 실패 감지 요구를 쓰기 방식에서 **분리**해, 스키마 결정과 무관한 별도 항목으로 처리한다 | 트랜잭션 롤백은 "조용한 낡은 색인"을 만들며, 교체 기구로도 해결되지 않는 문제 |
| D-4 | 논쟁 없는 5개 권고(NFC / `innerHTML` 제거 / form 제출 / 2단계 헤더 검증 / 부팅 거부)는 **재심 없이 구현 계약에 편입**한다 | 반대 의견 없음. 재검토는 순수 조율 비용 |
| D-5 | `spawn_web_ui`는 스니펫 구분자 계약 확정 즉시 **병렬 착수**, `search_api`+`access_guard`는 **한 트랙으로 병합** | 파일 소유권 분리 성립 여부에 따름 |
| D-6 | 슬랙·드라이브 범위 확장 판단은 **이 워크플로우 밖으로** 넘기고, 그 결정을 기다리지 않는다 | 위키+PDF 색인 코드는 소스 확장 시에도 폐기되지 않음 |

## Assumptions

- **A-1**: 이 프로젝트는 계측 소재이며, 위 결정들은 실제 제품 착수 판단이 아니라 워크플로우 산출물이다 (`docs/00_IDEA.md` 상단 명시).
- **A-2**: 사용자는 tech_lead 판정을 받을 세션을 한 번 더 돌릴 수 있다. 불가능하면 D-2는 사용자 본인 판정으로 대체한다.
- **A-3**: "전 직원 열람 가능 소스"를 실제로 열거할 수 있는 상태다. 열거 자체가 불가능하다면 그것이 곧 red_team 지적의 답이며 D-1이 폐기 신호가 된다.
- **A-4**: 4개 spawn 에이전트가 각각 격리 worktree를 받을 수 있다. 아니면 전부 직렬.
- **A-5**: `sources.json` 스키마는 경로 목록 수준으로 단순하다고 가정했다. 소스별 인증 정보가 들어가야 한다면 access_guard 재검토 필요.

## Risks

### Critical

- **모든 착수 조건이 동시에 걸려 아무도 시작하지 못한다.** 6개 에이전트 중 5개가 "착수 전에 ~를 고쳐야 한다"로 끝났고, 그 조건들의 소유자가 지정되지 않았다. 이 문서의 D-1·D-2가 그것을 닫는 유일한 장치다. 지금 상태에서 사용자가 이 워크플로우 결과만 보고 개별 에이전트에게 다시 물으면 조건이 순환한다.

### High

- **D-1이 "폐기" 방향으로 나올 경우의 처리가 정의되지 않았다.** allowlist 축소 후 남는 문서가 이미 다들 위치를 아는 공개 문서뿐이라면 이 제품은 성립하지 않는다. 그 경우 tech_lead 이하 모든 구현 계획이 무효이며, **이건 되돌리기 비용이 가장 싼 지점**이다 — 코드 한 줄 전.
- **FTS5 쓰기 방식을 결정 없이 착수하면 나중 수정 비용이 색인기 전면 재작성이다.** external-content 결합 여부가 스키마를 바꾼다. D-2를 건너뛰고 병렬 착수하면 indexer와 search_api가 서로 다른 스키마를 가정한 채 각자 완성한다.

### Medium

- **red_team의 범위 지적(슬랙·드라이브)이 D-6에 의해 유예된다.** 판단 자체는 옳지만, 유예된 항목은 조용히 사라지기 쉽다. 다음 워크플로우 입력값에 명시적으로 실어야 한다.
- **검색 품질 합격선이 여전히 없다.** `hit_count=0` 비율의 목표치가 없으면 출시 후 개선 판단이 감으로 흐른다. 구현 중에 정해도 되지만 정하긴 해야 한다.
- **`search_api` + `access_guard` 트랙 병합으로 한 세션의 작업량이 커진다.** 검색 API + 부팅 가드 + 2단계 헤더 검증을 한 컨텍스트에 넣으면 품질이 떨어질 수 있다. 순차 2회로 쪼개는 것이 대안.

### Low

- **`sources.json`이 없으면 색인기가 실행을 거부한다는 규칙이, 초기 개발 중 반복적인 마찰을 만든다.** 의도된 마찰이므로 완화하지 않는다. 개발용 샘플 파일을 리포에 함께 두면 해소된다.
- **web_ui의 제어문자 구분자 계약이 문서화되지 않으면 서버·클라이언트가 다른 문자를 쓴다.** 구분자 바이트값을 한 줄로 명시하면 끝.

## Recommended Next Actions

1. **`docs/01_INDEX_SCOPE.md`를 작성한다 (반나절, 코드 0줄).** 두 가지만 적는다 — (a) 전 직원 열람 가능 소스 실제 목록, (b) 그 목록만으로 답이 나오는 실제 사내 질문 5~10개. 5개를 못 채우면 이 MVP는 착수하지 않는다. 이 문서 하나가 tech_lead 선행조건과 red_team의 "추가 검증"을 동시에 닫는다.

2. **tech_lead에게 FTS5 쓰기 방식 판정만 단독으로 요청한다 (30분).** 질문은 하나로 고정: *"external-content FTS5에서 임시 테이블 교체가 트리거·rowid 결합을 깨는가? 깬다면 실제 테이블 단일 트랜잭션으로 확정한다."* 동시에 indexer의 실패 감지 요구는 스키마와 분리해 **"재색인 종료 시 처리 문서 수 로그 + 0건/예외 시 non-zero exit"** 로 계약에 못 박는다.

3. **위 1·2가 통과하면 `spawn_web_ui`를 즉시 병렬 착수시킨다.** 필요한 계약은 스니펫 구분자 바이트값 하나뿐이며 스키마 결정과 독립적이다. `search_api`+`access_guard`는 D-2 판정 후 단일 트랙으로, `indexer`는 `sources.json` 스키마 문자열 고정 후 병렬.

**지금 하지 말아야 할 일**: 슬랙·드라이브 커넥터 설계, 문서별 권한 모델 설계, 검색 품질 평가 하네스 구축, 4개 트랙 동시 착수.

## Next Agent

(없음 — 이 workflow의 마지막 단계)

## Artifacts To Update

outputs/chief_of_staff.md

## Handoff Notes

**사용자에게 넘기는 미결 항목 — 이 워크플로우 안에서 닫을 수 없는 것들:**

- **red_team의 범위 지적**(현재 MVP가 슬랙·드라이브를 커버하지 않음)은 PM/Founder CEO 판단이며 이 워크플로우에 해당 단계가 없다. 다음 호출 시 입력값에 명시적으로 실어야 하고, 그렇지 않으면 유예된 채 사라진다.
- **검색 품질 합격선**(`hit_count=0` 비율 목표치)이 정의되지 않았다. `docs/00_IDEA.md` 미해결 3번 항목이 그대로 남아 있다.
- **D-1이 폐기 신호를 낼 가능성**이 실재한다. 이 워크플로우는 "구현 가능한가"에 답했고 5개 에이전트가 모두 "가능하다"고 했지만, 그중 누구도 "축소 후에도 쓸 만한가"에는 답하지 않았다. 그 질문에 답하지 않은 채 착수하는 것이 이 프로젝트의 가장 큰 실행 리스크다.

**계측 맥락 주의**: 이 프로젝트는 `dev-preflight` fanout 기계를 live에서 계측하기 위한 소재다. 위 판단들은 워크플로우 산출물이며 사업 근거가 아니다. 판정 자체는 각 에이전트의 기준으로 독립적으로 내렸고, 계측 목적이 결론 방향을 제약하지 않았다.