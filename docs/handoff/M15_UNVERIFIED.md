# M15 — 미검증 등록부 (2026-09-03)

> **이 문서 하나가 "아직 실물로 확인하지 않은 것"의 정본이다.**
> 이 레포는 "적대적 검증은 거쳤으나 재검은 없다"를 여러 번 잊고 리비전을 발행했다. 그래서 분리해 적는다.
>
> **규칙**: 여기 있는 항목을 **근거로 코드를 고치기 전에** 먼저 실물로 재현하라.
> 재현되면 이 문서에서 지우고 판정 절·대장으로 옮긴다. **재현되지 않으면 주장 쪽을 고친다.**

## 등급 정의

| 등급 | 뜻 |
|---|---|
| **A. 코드만 읽었다** | file:line은 맞다. 그 경로가 실제로 그렇게 도는지는 **안 돌려 봤다** |
| **B. 적대적 검증만 거쳤다** | 다른 에이전트가 반증 시도까지 했으나 **사람도 나도 실물 확인은 안 했다** |
| **C. 주장만 있다** | 출처가 문서·보고서뿐 |

---

## 1. ~~`B-56` — `exec`/`mission` 자동 병합 3단~~ → **2026-09-04 재현 후 closed**

> 이 문서가 적어 둔 재현 방법(스크립트 없는 임시 레포 + 중간에 죽는 provider stub · live Claude 불필요)이
> **그대로 통했다.** 두 실패 모드 모두 `merged`였고, 부분 산출물이 develop에 병합됐다.
> 고친 것은 ⓐ(실패 신호 무시) 하나이고 ⓑ(빈 게이트 통과)는 의도된 계약이라 **사실 공개**만 붙였다.
> 상세: 판정 ⑯ ⓝ. **아래 원문은 재현 전 기록으로 남긴다.**

<details><summary>재현 전 기록</summary>

### (당시) `B-56` — `exec`/`mission` 자동 병합 3단 (등급 A)

대장 `B-56`으로 등재했고 **등급 A 유일 항목**인데, **재현하지 않았다.** 코드 경로만 이었다.

| 고리 | 근거 (읽은 것) | 안 해 본 것 |
|---|---|---|
| ⓐ non-zero 종료가 실패로 안 접힌다 | `claudeCliProvider.ts:94-104` · `sessionRunner.ts:90-97` | **Claude를 실제로 죽여서** outcome이 무엇이 되는지 |
| ⓑ 빈 게이트가 통과한다 | `machineGate.ts:77-106` (`[].every()===true`) | 스크립트 없는 레포로 `runMachineGate` **실행** |
| ⓒ 그 통과로 바로 push | `mergeCoordinator.ts:57-72` | `mission`을 실제로 돌려 **병합까지 가는지** |

**왜 위험한가**: 셋이 다 참이어야 "부분 코드가 검증 0회로 병합된다"가 성립한다. 하나라도 끊기면 등급이 내려간다.
**어떻게 확인하나**: 스크립트 없는 임시 레포 + 중간에 죽는 코더 provider stub으로 `runSession`을 돌려
`outcome.status`와 push 여부를 본다. **live Claude는 필요 없다** (provider는 주입 가능한 seam이다).

</details>

## 2. ~~`B-59` — 쓰기 경계 미집행~~ → **2026-09-04 재현 후 closed**

> 정적·동적 둘 다 확정했다. 정적: `ownership`이 `settings.json` 어디에도 없다(경로 문자열 검색 `false`) ·
> `Edit`/`Write`가 **경로 제약 없이** allow에 있고 `permissionMode`는 `acceptEdits` · `hookDenyPatterns`
> 소비자 0. 동적: 담당을 선언한 세션이 담당 밖 파일을 쓰고 **`merged`** 됐다.
> → `runSession`이 diff에서 담당 밖 변경을 찾아 **리뷰·승인·병합 앞에서** 멈춘다(`ownership_violation`).
> 상세: 판정 ⑯ ⓞ.
>
> **자기 정정 하나**: 첫 probe가 "settings에 hookDeny 흔적 있음"을 냈는데 **오탐이었다**(그 `curl`은
> `T2_policy` → `ask` 규칙에서 온 것). 부분문자열 판정이 틀렸고 확인해서 잡았다.

### 그래도 남은 미검 (이 수정이 덮지 않는 것)

- **Claude Code 권한 층의 end-to-end 검증**은 여전히 없다 — 컴파일한 `allow`/`deny`/`--settings`를
  설치된 CLI가 **의도대로 해석하는지** 안 돌려 봤다. `permissionCompiler.ts:10-12` 주석 자신이
  그것을 미검으로 표시한다. 새 검사는 **사후**(병합 전)이지 쓰기 자체를 막는 것이 아니다.
- **`hookDenyPatterns` 4건은 여전히 소비자 0이다.** `curl … | sh`·`rm -rf /`는 `deny`로 표현되지
  않아 그 가드가 붙기 전까지 비어 있다. (`curl` 자체는 `ask`라서 비대화 실행에서 어떻게 되는지도 미검.)
- **접두 매칭의 한계**: `src/*.ts` 같은 중간 와일드카드는 못 다룬다(지금 계약에 없는 모양이다).

<details><summary>재현 전 기록</summary>

### (당시) `exec`/`mission` 쓰기 경계 미집행 (등급 B)

`ownership`·`hookDenyPatterns`가 컴파일 후 소비자 0인 것은 **grep으로 확정**했다(정적 사실).
**미검은 그 결과다**: 에이전트가 실제로 소유 경로 **밖에 쓸 수 있는지**를 돌려 보지 않았다.
`--settings`로 넘어가는 Claude Code의 allow/deny가 얼마나 막아 주는지는 **CLI 버전에 달렸고**,
`permissionCompiler.ts:10-12` 주석 자신이 그 end-to-end 검증을 **미검으로 표시**하고 있다.

</details>

## 3. ~~수색 보고서 잔여 11건~~ → **2026-09-04 전수 검증 완료 · 대장 `C-150` closed**

> **8건 확인 · 3건은 이미 닫혀 있었다(M15의 앞선 슬라이스) · 반증 0.**
> 대장 `B-60`~`B-65`·`C-156`·`C-157`로 등재했다. 판정 ⑯ ⓟ에 방법과 함께 표로 있다.
>
> **수색 워크플로 19건이 전부 맞았다.** 그것이 그 워크플로를 신뢰할 근거이지 **다음 보고서를 무검증으로
> 받을 근거는 아니다** — 같은 M15가 보고서에 없던 거짓 안내를 스스로 셋 만들었다(`B-58`).
>
> **이 중 둘은 코드 대조만 했다**: `B-64`(프로세스 상한 리셋)·`C-156`(elapsedMsUsed) — kernel fixture로
> 관측하지 않았다. 나머지 6건은 재현했다.

<details><summary>검증 전 기록</summary>

### (당시) 수색 보고서 잔여 11건

`M15_DEFECT_SWEEP_REPORT.md`의 19건 중 **아직 재검 없는 것**:

```
B-2  B-3  B-6  B-7  B-8  B-9  B-10        (PART 1 B급)
C-1  C-2  C-3  C-4                        (PART 1 C급)
```

**갱신**: `B-4`(design 계약에 production 호출자 0)는 **2026-09-03 Codex 검수에서 독립 확인됐다** —
`validateDesignArtifacts`의 유일한 비-테스트 호출자가 `buildDesignHandoff`이고 그 함수도 호출자 0이다.
따라서 잔여는 **12건 → 11건**이다. (`C-2`는 M15의 `B-1` 수정이 오히려 넓혔다 — 대장 `B-58` ⓕ.)

**신호이지 증명이 아님**: M15가 재검한 5건(`A-1`~`A-4`·`B-1`·`B-5`)과 `B-4`는 전부 보고서대로 재현됐다.
그렇다고 나머지 11건이 참인 것은 아니다. **같은 세션이 보고서에 없던 거짓 안내를 새로 세 개 만들었다**(`B-58`).

</details>

## 4. Codex 1차 감사 중 반박을 거치지 않은 주장

2차에서 **8건만** 반박 대상으로 올렸다. 아래는 1차 Codex 주장 그대로이고 **재검 없음**:

| 주장 | 출처 | 등급 |
|---|---|---|
| `run`의 MCP 결속 tool profile이 이 경로에서 일괄 거부된다 | `runWorkflow.ts:692-704` | A |
| Next Actions가 비면 "MVP의 첫 기능 하나를 구현한다"를 **지어낸다** | `taskPrompt.ts:72-78` | A |
| task-prompt가 게이트 통과 **후** 문서를 다시 읽어 경합 창이 있다 | `taskPrompt.ts:49-69` | A (코드 주석이 이미 한계로 적고 있다) |
| `accounting.elapsedMsUsed`가 합계가 아니라 running max | 수색 `C-3` | B |
| 내보냈지만 비-테스트 호출자 0인 export 18~24건 | 내 스캔 24 · Codex 18 | A (**두 목록이 서로 다르다 — 합집합도 미확정**) |

## 5. `P1` live 계측이 재지 않은 것

- **4단계(`dev-handoff`)는 0회.** `task_prompt` 단계라 파이프라인 경로로만 닿는다(F3).
- **비용 대조군이 없다.** 첫 3단계 live라 "이 수치가 정상인지" 잴 기준선이 그 run 자신뿐이다.
- **`C-153`의 70k 고정 바닥 구성을 가르지 못했다.** 하네스 공통 프롬프트인지 `claude-code` 자체
  system prompt·도구 스키마인지. **빈 프롬프트 1회 호출 대조**가 선행이고, 그 전에는
  "프롬프트 영어화로 1.46배 절감"을 **주장할 수 없다.**
- **파이프라인 상태기는 그 run이 안 건드렸다**(pipeline absent).

## 6. 스위트 자체 (대장 `C-151`)

`test:core` 683~686건 중 **매 실행마다 다른 2~10건**이 실패한다(전부 `src/tools/` spawn 타임아웃).
`stash`한 HEAD에서도 8건 실패해 코드 변경과 무관함은 확인했으나, **왜 부하에서만 깨지는지는 안 팠다.**
acceptance도 겹쳐 돌리면 244/28, 단독이면 272/0이다. **즉 "전체 green"이 handoff 게이트로 기능하지 않는다.**

## 7. 개선 슬라이스가 **닫지 않은** 것 (2026-09-03 `B-57`·`B-58` 수정 후)

- ~~`B-56`~~ · ~~`B-59`~~ **둘 다 closed**(2026-09-04 재현 후). 남은 미검은 위 §2 "그래도 남은 미검" —
  **Claude Code 권한 층 end-to-end**와 **`hookDenyPatterns` 소비자 0**이고, 둘 다 새 사후 검사가 덮지 않는다.
- **`C-154` 잔여 4종**: 자리표시자 아이디어 통과 · 아이디어가 checkpoint에 결박되지 않음 ·
  사용자 조종 취소 전이 없음 · 단독 task prompt에 경로 루트 선언 없음. (ⓐ `init` 안내만 닫혔다.)
- **`C-155`** `briefGenerator`의 `deps` 조용한 버림 — `mission` 실사용 전.
- **`B-54` 잔여분**: 승인 시점 바이트를 어디에도 갖고 있지 않으면 여전히 나갈 길이 없다.
  안내가 이제 그 사실을 그대로 적을 뿐이다.
- **`guidance.test.ts`가 재지 않는 것**: `--provider` 누락(승계로 무해해졌으므로 검사하지 않는다) ·
  안내가 가리키는 **상태**가 실제로 도달 가능한지(그건 여전히 사람이 실행해 봐야 안다).

## 8. 이 세션이 스스로 틀린 기록 (같은 무게로 남긴다)

| 내가 주장한 것 | 실제 |
|---|---|
| "빈 게이트는 의도된 것이고 공개된다" | **틀렸다** — 공개는 `commands/exec.ts:79` 한 곳뿐, mission·parallel은 침묵 |
| "brief 조용한 버림은 영향 낮음" | **틀렸다** — `deps` 스칼라가 `undefined`가 되어 의존성 없음으로 실행된다 |
| "활성 파이프라인에 종결 상태가 없다" | **과했다** — 게이트가 '폐기'를 내면 terminal에 닿는다(사용자 조종은 여전히 불가) |
| drift 안내 ⓐ "되돌리면 이어집니다" | **그 상태에선 거짓** — replay 분기로 재거부(실행 확인) |
| `A-1`/`A-2` 보강 팔 "재판정 없이는 계속 거부" | **거짓이었다** — 같은 명령 하나로 증발(당시 실측으로 잡아 철회) |

**공통 원인 하나**: 코드는 "모든 호출자를 grep"하고 고쳤는데 **문자열은 그러지 않았다.**
