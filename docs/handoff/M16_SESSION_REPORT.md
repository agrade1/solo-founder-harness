# M16 세션 보고 — 머지 정상화 · 필수 3건 · 사용자 실사용이 잡은 3건

> 2026-09-07 ~ 09-09 · main `28de769` · 대장 **125건**(등급 A **0**)
> 착수 문서였던 `M16_ORCHESTRATION_KICKOFF.md`를 이 문서가 대체한다.

## 0. 30초 요약

**킥오프의 인과 하나가 틀렸고, 사용자 실사용이 내 버그 하나를 잡았다.**

- 킥오프는 live 4단계의 선행 조건을 `B-47`로 적었다. `B-47`은 닫혔고 **mock 4단계는 완주한다**.
  그런데 **live는 1단계 게이트를 못 넘는다** — 막은 것은 코드가 아니라 **사람의 오프라인 작업**이다(`C-160`).
- 사용자가 실제 크레딧으로 돌린 run이 **내가 이 세션에 만든 결함**을 잡았다(`C-163`).
  `B-48` 수정이 예산 예외를 리서치 catch에 걸리게 만들어, 예산 소진인데 "키·네트워크를 고치라"고 안내했다.
- 사용자 지적 둘을 그 자리에서 닫았다: **아이디어 문서 인수**(`C-162`)와 **확인 요청서**(`C-164` · 66KB→5.8KB).

## 1. 지반 (main 기준 실측 · 전부 단독 실행)

| 항목 | 값 |
|---|---|
| main | `28de769` |
| suite | typecheck 0 · core **718** · exec **662** · acceptance **272** · `npm test` 전체 체인 exit 0 |
| 대장 | **125건** · 등급 A **0** |
| live | 1단계 **미완주**(게이트) · 4단계 **0회** · `'진행'` 판정 **누적 0회** |

**스위트는 이제 겹쳐 돌려도 안전하다**(`C-151`) — 세 suite 전부 lock을 지나고, 겹치면 exit 2로 거부된다.

## 2. 닫은 것

### 필수 (킥오프 §2.1)
| id | 무엇 | 검증 |
|---|---|---|
| `B-47` | `reject`가 인쇄하는 재실행 명령이 그 자리에서 거부되던 막다른 길 | fixture 재현 → mutation 2 RED |
| `B-48` | 예산 검사를 step 경계 → **모든 모델 호출 직전**으로 | 재현 700→**1,200**(+71%) → 수정 후 800 |
| `B-64` | `maxProcessesPerRun`이 attempt 롤오버마다 리셋 | kernel fixture 관측 32→**28** → mutation 1 RED |
| `C-156` | **반증** — 경과 상한이 wall-clock이라 max가 맞다 | 코드가 아니라 **주장을 고쳤다** |
| live 4단계 | **미완** → `C-160`으로 원인 등재 | §4 참조 |

### 개선 A~C
`C-151`(스위트 비결정성) · `B-51`(아이디어 문서의 게이트 우회) · `C-153`(70k 바닥 **가름 완료**) ·
`C-155`(스칼라 `deps`가 '의존성 없음'이 되어 선행 완료 전 실행) · `C-154ⓐⓒ`(**승인 영수증이 심사받은
입력을 안 담았다**) · `B-54`(형제 안내 + 스냅샷 **"안 한다"로 결정 종결**)

### 사용자 실사용이 낳은 것
| id | 무엇 |
|---|---|
| `C-162` | **`init`이 작업 폴더의 아이디어 문서를 인수한다** — 후보 6종(`idea.md` 등). 옮기지 않고 복사하고, 정본이 어디인지 인쇄한다 |
| `C-163` | **예산 소진이 리서치 실패로 둔갑**해 틀린 원인을 안내했다 — 내가 만든 결함, 사용자 live가 잡았다 |
| `C-164` | **확인 요청서** `outputs/CHECKPOINT_REPORT.md` — live 산출물 실측 **66KB → 5.8KB(91% 감축)** |

## 3. 반증·정정한 주장 셋 (이 세션의 성과 중 절반은 이것이다)

1. **`C-156`** — "합계여야 하는데 max다"는 틀렸다. 상한이 wall-clock이라 max가 맞고, 합계로 바꾸면
   병렬 task의 wall time을 중복 계산한다. **코드가 아니라 주장을 고쳤다.**
2. **`C-153`의 "영어화로 1.46배 절감"** — 과대주장이었다. 빈 프롬프트 대조 2회로 바닥을 갈랐다:
   **36,148**(provider 자체) + **~4,016**(이 레포 `CLAUDE.md`) + **~29k**(하네스 몫).
   영어화가 닿는 것은 ⓒ뿐이라 상한이 **약 13%**다. 대신 더 싼 레버가 나왔다(`C-159`).
3. **`C-153`의 "98%·편차 9.5%"** — 비평 루프·게이트 점프가 **없는** run에 한정된 수치다.
   이번 live는 편차 **352%**(68,846~311,236) · 입력:출력 **10.7:1**.

## 4. live 4단계 — 막은 것은 코드가 아니다 (`C-160`)

`founder_ceo`가 **'검증'을 두 번** 내고 되돌림 예산이 소진돼 `ceo_decision_verify`로 멈춘다
(M16 실측: 호출 16회 · 입력 1,582,482 · 출력 148,213).

CEO가 요구한 것은 **3시간짜리 실물 테스트**(카드 CSV 가맹점 문자열 식별률 · 기존 무료 대안 화면 확인)이고
**하네스는 그 증거를 만들 수 없다.** 수행하지 않은 채 `## Decision`을 '진행'으로 고치는 것은 **검증 위조**라
하지 않았다. `B-50`의 사람 검증 레버는 **정상 작동한다** — 안내가 지시하는 경로도 참임을 실측했다.

**선택지 둘**: ⓐ 사람이 실물 테스트 후 `## Decision`을 고친다(모델 호출 0회로 재판정)
ⓑ 게이트 없는 경로로 2~4단계 **기계 동작만** 계측한다(그것은 "live 4단계 완주"가 아니다).

**`'진행'` 판정은 누적 0회다.** 이번 아이디어는 P1보다 구체적이었는데도 3라운드 연속 '검증'이었다 —
가설 ⓐ(아이디어가 약했다)는 **약해졌지만** ⓑ(기준 도달 불가)와 아직 못 갈랐다.

## 5. 이번 세션이 비싸게 배운 것 (함정 30~33)

30. **`--delete-branch`를 안 쓰면 base 재타겟도 안 된다.** 둘은 같은 스위치가 아니다.
    스택 #116~#122를 순서대로 머지했는데 #117~#122가 각자의 스택 base로 들어가 **main에는 #116만** 반영됐다.
    → **머지 후 main의 tree를 스택 tip과 대조하라.**
31. **예외를 안정 코드로 바꾸는 catch는 자기 도메인 밖의 예외를 통과시켜야 한다.**
    안 그러면 나중에 붙는 모든 횡단 관심사(예산·취소·타임아웃)가 그 자리에서 도메인 오류로 둔갑한다(`C-163`).
32. **문서를 쓰는 쪽에서 보면 다른 결함이 보인다.** 테스트용 아이디어 문서를 쓰다가 `C-161`(템플릿과
    추출기의 형식 불일치)을 찾았다 — 코드만 읽어서는 안 보이는 자리였다.
33. **기능을 추가하면 그 기능의 안내도 형제다.** `C-162` 초판이 문서를 담아 놓고도 "채우세요"라고
    지시했다(함정 27의 재발). **실물로 한 번 돌려서** 잡았다.

## 6. 다음 세션이 이어서 할 것

**사용자 결정 대기 중**: 확인 요청서(`C-164`) 형식 피드백 → 그다음 **MCP 서버**(①-A).
MCP는 이 보고서를 실어 나르는 층이라 형식이 틀어지면 두 번 고친다 — 그래서 순서를 그렇게 잡았다.

**업계 조사 결론**(2026-09): CLI 전용은 소수파다. DeepSeek Harness는 web-UI 우선, Cline은 IDE 확장,
TrueForge는 코어 서버가 스트리밍·승인. **AG-UI**가 에이전트↔프론트엔드 HITL interrupt 표준으로
자리잡는 중이다. 다만 harness 생태계는 승인을 **제약 집행 문제**로만 다루고 **사람이 읽을 UI/UX는 공백**이다
— `C-164`가 그 공백을 메우는 첫 걸음이다.

**열린 항목 중 우선순위**: `C-159`(레포 `CLAUDE.md`가 모든 agent 호출에 4k씩 실린다 · 한 줄 수정) ·
`C-152`(병렬 fanout — **`B-48`이 새 선행 조건이 됐다**: 병렬화하면 초과분이 호출 1개→N개) ·
`C-158`(`elapsedMs`가 호출자 값) · `C-161`(템플릿·추출기 형식 계약) · `C-154ⓑⓓⓔ`.

## 7. 환경 변경 (이 세션이 사용자 머신에 한 일)

- **codex CLI 업그레이드**: `0.146.0-alpha.3` → **`0.154.0-alpha.8`**(사용자 승인).
  `gpt-6-astra` 사용을 위해서였고 동작 확인했다.
- **정직하게**: 처음에 **플랫폼 전용 패키지**(`@openai/codex@alpha-darwin-arm64`)를 깔아 `bin`이 없어
  `codex` 명령이 사라졌다. 원인을 찾아 `@openai/codex@alpha`로 복구했다. 리뷰는 앞으로 `gpt-6-astra`로 한다.

## 8. 아직 못 한 것 (숨기지 않는다)

- **live 4단계 0회** · **`'진행'` 판정 0회** — §4.
- **`B-51` 가드의 live 재현 실험 안 함** — 프롬프트에 실린다는 것만 관측했다.
- **`C-153`의 ⓒ(~29k)는 뺄셈으로 얻은 값**이고 직접 계측하지 않았다. 표본도 각 1회다.
- **live 8번 호출이 바닥의 4.5배**(311,236)인 **원인 미규명**.
- **`run_state.timings`가 비어 있다** — 이유 확인 안 했다.
- **Codex 적대적 리뷰 0회** — 이 세션의 수정들은 **자체 mutation 검증만** 거쳤다(총 9종, 전부 RED 확인).
